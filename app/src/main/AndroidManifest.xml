<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.automobileassist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".e_service_centers_proflle"></activity>
        <activity android:name=".find_emergency_service" />
        <activity android:name=".emergencycenter_register" />
        <activity android:name=".emergencyservicelogin" />
        <activity android:name=".emergency_servics_search" />
        <activity android:name=".GarageLoginForm" />
        <activity android:name=".GarageRegisterForm" />
        <activity android:name=".GarageSearch" />
        <activity android:name=".GarageOwnerMenu" />
        <activity android:name=".SubMenu" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>