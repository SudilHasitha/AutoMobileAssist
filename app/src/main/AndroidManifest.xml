<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.automobileassist">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view_Vehicles_Customers"></activity>
        <activity android:name=".Login_Form" />
        <activity android:name=".Profile_ui" />
        <activity android:name=".Search_Vehicle" />
        <activity android:name=".Vehicle_Details_Form" />
        <activity android:name=".Regtaion_form" />
        <activity android:name=".Salesmen_Customer" />
        <activity android:name=".Registaion_or_Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>